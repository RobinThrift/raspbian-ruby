# Ruby on Raspian (for Raspberry Pi 2)
Ruby built for Raspian on the Raspberry 2.

## Usage
- `make build` will build the image
- `make push` will push the image to docker hub
- `make test` tests the image
